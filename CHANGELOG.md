## 0.0.2

* Added missing `repository` and `issue_tracker` URLs to `pubspec.yaml` to improve `pub.dev` indexing and links.
* Updated standard MIT License information.
* Re-architected rendering layout to automatically align data models under their respective columns, preventing previous RenderFlex Overflow issues.
* Abstracted standard calculations manually built into `LiquidLevelChart` into a separate `ChartCalculator` utility class.

## 0.0.1

* Initial release. Includes `LiquidLevelChart` and `LiquidData` model for creating interactive level charts.
